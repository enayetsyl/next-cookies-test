export default function HomePage() {
  return (
    <main>
      <h1>Welcome</h1>
      <p>This is a minimal Next.js frontend that uses an HttpOnly cookie to protect <code>/dashboard</code>.</p>
      <p>Use the <a href="/login">Login</a> page, then visit <a href="/dashboard">Dashboard</a>.</p>
    </main>
  );
}
